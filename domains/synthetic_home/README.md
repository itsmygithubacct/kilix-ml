# Synthetic Home Tool Schema

`tools.json` is the closed-object tool schema pack used for synthetic-home schema-reading examples. It is copied from the immutable 14-v4 training corpus export and contains tool definitions only; it contains no request examples or evaluation data.
